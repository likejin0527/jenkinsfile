node {
   //def mvnHome
   stage('checkout') { // for display purposes
       git credentialsId: '1ffbeb0e-ac97-4e3c-889f-de9eebec96fc', url: 'https://github.com/likejin0527/java.git'
   }
   stage('mavenBuild') {
       withMaven(jdk: 'JDK9', maven: 'MAVEN') {
    // some block
}
   }
   stage('deploy') {
      echo 'ddddd'
      echo 'sbsb'
   }
   stage('test') {
      echo 'test...'
      echo 'slslsl'
   }
}